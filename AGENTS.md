# Codex 작업 규칙

## 프로젝트 규칙

- 모든 작업을 시작하기 전에 `CLAUDE.md`를 읽고 그 규칙을 적용한다.

## Git 동기화

- 작업 시작 시 `git fetch --prune origin`을 실행하고 현재 브랜치와 upstream의 ahead/behind 상태를 확인한다.
- 검증이 끝난 하나의 논리 작업 단위마다 해당 작업에서 변경한 파일만 커밋하고 현재 upstream으로 즉시 푸시한다. 커밋 가능한 변경을 30분 넘게 로컬에만 두지 않는다.
- 작업 시작 후 5시간마다, 그리고 최종 응답 전에 다시 fetch한 뒤 `HEAD`와 upstream의 커밋이 같은지와 작업 트리에 미커밋 변경이 남았는지 확인한다.
- 로컬이 ahead이면 현재 작업의 검증된 변경을 커밋·푸시한 뒤 다시 확인한다. 원격이 ahead이거나 양쪽이 분기되었으면 병합이나 rebase 전에 상태를 사용자에게 보고한다.
- 사용자가 만든 관련 없는 변경은 커밋하지 않는다. 비밀정보와 `.env`는 커밋하지 않으며 force push는 하지 않는다.
