# PLAN — POSCO Future M News Intelligence

`PRD.md` v0.5를 실행 단위로 분해한 작업 계획서다. 각 항목 뒤 `(F1.1)` 형태는 PRD 대응 조항이다.

| 항목 | 내용 |
|---|---|
| 기준 문서 | `PRD.md` v0.5 |
| 작성일 | 2026-09-02 |
| 구조 | `backend/` (파이썬 단일 파일) + `frontend/` (정적) + SQLite→Supabase + OpenAI |
| 현재 상태 | **M0~M5 구현·검증 완료** · M6 운영 강화 미착수 · M7 최종 점검 부분 완료 |

## 진행 현황 (2026-09-02)

| 단계 | 상태 | 검증 결과 |
|---|---|---|
| M0 기반 | ✅ 완료 | 스키마 2종(Postgres/SQLite), 키워드 29개·언론사 33개 시드 |
| M1 수집 파이프라인 | ✅ 완료 | **재조회 비용 0 검증 통과** — 신규 0건 실행 HTTP 9회 = 소스조회 9 + 신규 0 |
| M2 LLM 분석 | ✅ 완료 | `gpt-5.6-luna` 기사당 호출 1회로 요약·관점·키워드·감성·SWOT 동시 수신 |
| M3 웹 아카이브 | ✅ 완료 | 다중 선택 필터(그룹 내 OR / 그룹 간 AND) 및 칩 중복 0건 확인 |
| M4 시세 티커 | ✅ 완료 | 주가 2종 + 환율 4종 6/6 조회 성공. 소스 2개 폴백 구현 |
| M5 텔레그램 | ✅ 발송 확인 | `chat_id=7575759042`(개인 DM). 시험 메시지 + 실제 기사 알림 발송 성공 |
| M6 운영 강화 | 🔶 부분 | 네이버 API 연동·검증 완료. 운영 대시보드·관리자 화면·경보 미착수 |
| M7 최종 점검 | 🔶 부분 | 보안·중복·재조회 비용 검증 완료. 성능·지표 측정은 데이터 축적 후 |

**현재 운영 중**: `python backend/main.py run` — 60초 폴링 루프 + API 서버(http://127.0.0.1:8000).
초기 백로그(~500건) 해소 중이며 억제 모드 유지, 안정화되면 자동으로 알림 활성화.

내장 검증(`python backend/main.py selftest`) **55개 항목 전부 통과**.

---

## 0단계. 착수 전 확정 (2026-09-02 확정 완료)

- [x] **0-1. 스택 확정** (§8.2, §12-10) — **`backend/main.py` 파이썬 단일 파일 + `frontend/` 정적 HTML·CSS·JS**
  - CLAUDE.md의 "파이썬 단일 파일" 규약을 우선. PRD 초안의 Cloudflare Workers + Next.js는 채택하지 않는다
- [x] **0-2. 파이썬 라이브러리 승인** — 아래 전부 승인됨
  - `requests` · `feedparser` · `beautifulsoup4` · `readability-lxml` · `openai` · `supabase` · `python-dotenv` · `fastapi` · `uvicorn`
  - **이 목록 밖의 라이브러리는 다시 확인받는다** (CLAUDE.md 규약)
- [x] **0-3. 시세 데이터 소스 확정** (F9, §12-9) — **무료 공개 소스**. 키 발급 불필요
  - 대가로 지연·중단 리스크를 안으므로 **F9.2 폴백(마지막 값 유지 + 지연 배지)이 필수**가 된다
  - 실제 구현: 네이버 금융 비공식 엔드포인트 2종(prices / api.stock)을 순서대로 시도
- [x] **0-4. DB 진행 방식 — 로컬 SQLite 우선, Supabase 나중 이전**
  - PRD §8.2는 Supabase를 명시하므로 **이전 비용을 0에 가깝게 만드는 설계가 전제 조건**이다
  - [x] 저장소 계층(`Storage`)을 인터페이스로 분리하고 `SqliteStorage` / `SupabaseStorage` 두 구현을 둔다
  - [x] 파이프라인 코드는 **SQL을 직접 쓰지 않고** 저장소 계층만 호출한다
  - [x] Postgres 전용 타입을 피한다: `text[]` → JSON 문자열, `vector` → JSON 배열 + 애플리케이션 코사인 계산
  - [x] `schema.sql`(Postgres/Supabase용)과 `schema_sqlite.sql`을 **같은 논리 스키마**로 유지한다
  - [x] `.env`에 `DB_BACKEND=sqlite|supabase` 스위치를 둔다. 키가 채워지면 값만 바꿔 전환
- [x] **0-5. `pgvector` 미사용 확정** (F5) — 임베딩은 JSON 배열로 저장, 코사인은 애플리케이션 계산. 4단계 후보 건수가 적어 성능 문제 없음 (F2.2)
- [ ] **0-6. 배포 위치 확정** (§12-11) — 로컬 상시 실행 / 서버 / 클라우드. **M6 착수 전까지 확정 필요**
- [x] **0-7-a. §12-2 텔레그램 대상 확정** — 개인 DM(`7575759042`). 그룹방 전환 시 알림 임계값 재검토
- [ ] **0-7-b. 나머지 미결정 §12-1,3,4,5,6,7,8** — 웹 공개 범위, 지역 키워드 확정, 네이버 API, 기존 자산 연계, 본문 보관 기간, LLM 예산 상한, 수정 기사 재처리

---

## M0. 기반

- [x] **M0-1. `backend/schema.sql` 작성** (F5)
  - [x] `articles` — `url_source`/`url_canonical` UNIQUE, `sentiment`, `keywords`, `group_companies`, `categories`, `title_embedding`
  - [x] `url_ledger` — 영구 제외 원장 (F1.1)
  - [x] `run_state` — 부트스트랩/복구 모드 (F1.1)
  - [x] `summaries` — `summary_text` + `perspective_text` 분리 (F4.1)
  - [x] `swot_analyses` — S/W/O/T 점수 4개 + 근거 4개 + `total_score` (F4.4)
  - [x] `press_outlets`, `keyword_sets`, `feed_sources`, `notifications`, `collection_logs`
  - [x] `market_quotes` — 주가·환율 캐시 (F9)
  - [x] 인덱스: `published_at`, `importance_score`, `url_source_aliases`/`keywords`/`group_companies`/`categories` gin
- [x] **M0-2. RLS 정책 적용** (F5) — 공개 읽기는 `articles`/`summaries`/`swot_analyses`/`market_quotes`/`press_outlets(approved)`, 쓰기는 service role만
- [x] **M0-3. `keyword_sets` 시드 입력** (F1.3) — 그룹사·지역·산업·정책 4개 카테고리 29개. **코드 하드코딩 아님, DB 관리**
- [x] **M0-4. `press_outlets` 초기 매핑** (F2.3) — 주요 언론사 33개 도메인 → 이름·tier
- [x] **M0-5. 환경변수 로더 구현** (§6, `env-secret-guard`)
  - [x] 필수 키를 시작 직후 한 번에 전부 검증하고 없으면 즉시 종료
  - [x] 로그·예외에 키 값을 출력하지 않는다
  - [x] 선택 키는 기본값과 함께 로드. `.env` 인라인 주석(`60  # 폴링 주기`)도 안전하게 파싱

---

## M1. 수집 파이프라인

**완료 기준**: 1분 주기 실행, 중복 저장 0건, **신규 0건 실행의 추가 HTTP 요청 0회**, 실행 20초 이내 (§9) — 실측 1.4~3.8초

- [x] **M1-1. 소스 수집** (F1.2)
  - [x] Google News RSS — 등록은 되어 있으나 **기본 비활성**. 링크가 JS 토큰이라 원문 URL 해제가 불가능하고, 우회는 §7-4 위반
  - [x] 네이버 검색 API — 키 있을 때만 활성. `originallink`가 원문이라 해제 불필요. **직접 크롤링 금지** (§7-4)
  - [x] 언론사 자체 RSS 9종 — `feed_sources` 테이블 등록. 코드 수정 없이 추가 가능 (§6 확장성)
  - [x] RSS 소스는 키워드로 질의 불가 → 수집 후 로컬에서 `keyword_sets` 기준 필터
- [x] **M1-2. 게이트 순서 구현 — 순서 자체가 요구사항** (F1.1)
  - [x] G0 실행 내 중복 제거 (in-memory Set)
  - [x] G1 seen-set 캐시 대조 (최근 72시간, 성능 계층일 뿐 정확성에 관여하지 않음)
  - [x] G2 `url_source` 배치 DB 조회 — 전체 기간 무기한. `articles` + alias + `url_ledger`
  - [x] G2.5 신선도 컷오프 — RSS `pubDate` 사용, 네트워크 호출 없음
  - [x] G3 리다이렉트 해제 → `url_canonical` (여기서 처음 HTTP 발생)
  - [x] G4 본문 추출 (G3 응답 재사용, 추가 요청 없음)
  - [x] G5 중복 그룹 판정
  - [x] G6 LLM 분석
  - [x] **검증**: 재실행 시 기존 6건 전부 G1 탈락, HTTP = 소스조회 9 + 신규분만
- [x] **M1-3. 이중 키 구조** (F1.1) — `url_source`(네트워크 없이 계산, 판정 키) / `url_canonical`(해제 후, 표시용). 둘 다 UNIQUE
- [x] **M1-4. URL 정규화 2시점 분리** (F2.1)
  - [x] (A) 게이트 이전: 추적 파라미터 제거, 소문자화, 기본 포트 제거, 후행 슬래시 정리 — 리다이렉트 해제 금지
  - [x] (B) 게이트 통과분: 리다이렉트 최대 홉 + 타임아웃, `<link rel="canonical">`/`og:url` 우선, 동일 정규화 재적용
- [x] **M1-5. 중복 판정 4단계** (F2.2)
  - [x] 1단계 정규화 URL 완전 일치
  - [x] 2단계 본문 SHA-256 해시 일치
  - [x] 3단계 제목 정규화 후 문자열 유사도 ≥ 0.9 **AND** 발행 시각 차 ≤ 24h
  - [x] 4단계 잔여 후보만 임베딩(`text-embedding-3-small`) 코사인 ≥ 0.92 AND 24h. 벡터는 `title_embedding`에 캐시
  - [x] 제목 정규화: 말머리 제거(중첩 포함), 언론사·기자명 꼬리 제거, 전각→반각
  - [x] 대표 선정: ① 언론사 tier ② 본문 추출 성공 ③ 발행 시각 빠른 순
  - [x] alias 누적 → 다음 실행부터 G1 탈락
- [x] **M1-6. 신선도 컷오프 및 제외 원장** (F1.1)
  - [x] 6시간 이내 = 저장 O / 알림 O, 6~72시간 = 저장 O(`is_backfill`) / 알림 X, 72시간 초과 = 스킵
  - [x] 발행 시각 없음(`no_pubdate`) · 본문 추출 실패(`extract_failed`) → `url_ledger` 기록, 다음 실행부터 G2 차단
  - [x] `url_ledger.hit_count` 증가 로직
- [x] **M1-7. 부트스트랩 · 복구 억제 모드** (F1.1) — 최초 실행 및 30분 이상 중단 후 재개 시 1회 억제 모드. `run_state`로 관리
- [x] **M1-8. 분류 및 중요도 스코어** (F3)
  - [x] 카테고리 태깅 (그룹사/배터리·이차전지/지역/정책·법령/시장·주가) — 복수 허용. '지역'은 실제 사업장 지명으로만 판정
  - [x] 스코어: 퓨처엠 직접 +40(제목 +50), 그룹사 +25, 정책·규제·수사·사고 +20, 지역 사업장 +15, 주요 언론사 +10, 단순 시황 −15
  - [x] 0~100 범위 클램프
- [x] **M1-9. 수집 실패 처리** (F1.4) — 소스별 조회 실패 시 경고 로그 + `collection_logs` 기록. 소스 하나가 죽어도 나머지는 진행
- [x] **M1-10. 회귀 검증** (F1.1 검증 기준)
  - [x] 재실행 시 기존 기사 전부 G1/G2 탈락, 추가 HTTP 0회 — **실측 통과**
  - [x] `collection_logs.skipped_seen_count` · `http_request_count` 기록
  - [x] 정규화 함수 단위 테스트 (`selftest` [1]~[3])
  - [x] 1회 실행 처리 상한(`MAX_PROCESS_PER_RUN=40`) — 넘친 항목은 아직 DB에 없어 다음 실행에서 다시 후보

---

## M2. LLM 분석 (요약 · 키워드 · 감성 · SWOT)

**완료 기준**: 요약 성공률 90% 이상, **기사당 LLM 호출 1회** (§9, §10)

- [x] **M2-1. 본문 추출** (F4.5)
  - [x] Readability 1순위, 실패 시 BeautifulSoup 폴백
  - [x] 추출 실패 시 RSS `description` + 제목 → `summary_source = 'snippet'`. 요약을 포기하지 않는다
  - [x] 영구 실패 URL은 `url_ledger`에 `extract_failed` 기록
- [x] **M2-2. 통합 분석 호출 1회 구현** (F4 서두) — 요약·관점·키워드·그룹사·감성·SWOT을 JSON 강제 출력 1회로 수신
  - [x] 모델이 JSON 모드 미지원 시 자동으로 일반 모드로 전환 후 파싱
- [x] **M2-3. 요약 규격** (F4.1)
  - [x] 3~5문장, 한국어, 문장당 40자 내외
  - [x] `summary_text`(본문)와 `perspective_text`(`포스코 관점:`) 분리 저장
  - [x] `[언론사, 기자명]` 머리표는 저장하지 않고 표시 시점에 조립. 기자명 없으면 `[언론사]`
  - [x] 언론사명이 기자명 자리에 잡히는 경우 방어(`extract_author`)
  - [x] "요약불가" 탈출구 제공 → 실패 처리
- [x] **M2-4. 키워드 및 관련 그룹사** (F4.2)
  - [x] 키워드 최대 6개, 정규화(공백·특수문자 제거+소문자) 후 중복 제거
  - [x] 관련 그룹사를 `group_companies`로 별도 필드 분리
  - [x] 그룹사 판정 룰 기반 1순위, LLM 결과 보조 병합. 정규 목록 밖 회사명 폐기
  - [x] 계열사가 있으면 상위 개념 '포스코' 제거 (`normalize_group_list`)
  - [x] 그룹사로 표기된 값은 키워드에서 제외
- [x] **M2-5. 감성 태그** (F4.3) — `긍정`/`중립`/`부정`. 잘못된 값은 '중립'으로. 포스코 그룹 대응 필요성 기준
- [x] **M2-6. SWOT 분석** (F4.4)
  - [x] S·W·O·T 각 0~100 점수 + 1~2줄 근거
  - [x] `total_score = (S+O)-(W+T)` 를 0~100 정규화. 카드에는 이 값만 노출
  - [x] 근거 없는 항목은 점수 0 · `해당 없음`
  - [x] `summary_source = 'snippet'`인 기사는 SWOT 미생성
- [x] **M2-7. 비용 절감 장치** (F4.5, F4.6)
  - [x] 게이트 통과 신규 기사만 분석
  - [x] 중복 그룹 대표 1건만 호출
  - [x] `LLM_DAILY_LIMIT` 도달 시 중요도 상위 건만 처리
  - [x] 모델은 `LLM_MODEL` 환경변수로 주입. 코드에 모델명 하드코딩 없음
  - [x] `once N` 명령으로 실행당 LLM 호출 제한(검증용)
- [x] **M2-8. 실패 처리** (F4.5) — 지수 백오프 3회, 최종 실패 시 링크·제목만 저장. `analyzed_at` null = 재처리 큐
- [x] **M2-9. `token_usage` · `model` 매 건 저장** (F5)
- [ ] **M2-10. 대량 검증** — 데이터가 아직 7건 수준. 수집량 확보 후 수행
  - [ ] 요약 성공률 95% 이상(폴백 포함)
  - [ ] 중복 그룹 15건 → LLM 호출 1회 (실데이터 중복 케이스 축적 후)
  - [ ] 본문에 없는 고유명사·수치가 요약에 등장하지 않음 (샘플 20건 수동 검증)
  - [x] 재실행 시 기분석 기사 호출 0건 — **실측 통과**

---

## M3. 웹 아카이브

**완료 기준**: 레퍼런스 디자인 수준, 칩 중복 0건, 모바일 대응 (§9)

- [x] **M3-1. 백엔드 조회 API** — `/api/articles` `/api/filters` `/api/quotes` `/api/stats` `/api/articles/{id}`
- [x] **M3-2. 레이아웃 7영역** (F6.1) — 헤더 → 시세 티커 → 통계 바 → 필터 → 카드 그리드 → (운영 상태) → 푸터
  - [x] 딥네이비 `#16337A` 브랜딩, 라벨 스트립
  - [x] 통계 바: 전체 기사 수 / 오늘 수집 / 최근 수집 / 분석 대기 / 발송 실패
- [x] **M3-3. 다중 선택 필터** (F6.1, F6.1a)
  - [x] 그룹사·카테고리·언론사 = 다중 선택, 기간 = 단일 선택
  - [x] 결합 규칙: 같은 그룹 안 OR / 다른 그룹 사이 AND — **실측 통과** (퓨처엠 3 → 퓨처엠+홀딩스 4 → 퓨처엠 AND 시장/주가 2)
  - [x] 재클릭 시 해제, 선택 칩 채움 / 미선택 외곽선
  - [x] URL 쿼리스트링 동기화
  - [x] 전체 해제 버튼 + 현재 결과 건수 표시
  - [x] 키워드 자유 입력 검색
- [x] **M3-4. 포토카드** (F6.2)
  - [x] 상단 메타: 발행시각 · 언론사 배지 · 원문 열기(↗) · 즐겨찾기(☆, localStorage)
  - [x] 썸네일 (og:image, 로드 실패 시 제거)
  - [x] 제목 2줄 말줄임
  - [x] 요약: `[언론사, 기자]` 머리표 + 본문 + `포스코 관점:` 분리 박스
  - [x] ✎ 기자명
  - [x] 칩 행: 그룹사 칩(강조색) → `감성 · 점수` 칩 → 키워드 칩
- [x] **M3-5. 칩 중복 제거 — 렌더링 단계 2차 방어** (F6.2b)
  - [x] `dedupeChips` — 정규화 키 기준, 그룹사 칩이 있는 값은 키워드에서 제외
  - [x] 백엔드 `build_card`에서도 한 번, 프론트 `buildCard`에서 또 한 번
  - [x] 실데이터 7건 전수 확인 — **칩 중복 0건**
- [x] **M3-6. SWOT 배지 및 툴팁** (F6.2a)
  - [x] 카드에 종합 점수 배지
  - [x] 트리거: 호버 / mousemove(드래그 포함) / 포커스 / 클릭(모바일 탭)
  - [x] 툴팁: S·W·O·T 4행, 각 `라벨 · 점수 · 근거`
  - [x] 뷰포트 경계 감지 후 뒤집기, 스크롤 시 닫힘
  - [x] SWOT 없는 기사는 배지 미렌더링
- [x] **M3-7. 반응형 및 성능** (F6.2, F6.3) — 3열/2열/1열, 스켈레톤, 더 보기(20건 단위)
- [x] **M3-3a. 필터 접기/펼치기** — 필터 헤더 클릭 시 본문 토글. 활성 필터 개수 배지 표시, 접힘 상태 localStorage 기억. 항목이 많아져 추가 (2026-09-02)
- [x] **F8-a. 수동 URL 등록** — 웹 하단에 URL 입력 → `POST /api/analyze-url` → G3~G6(파이프라인과 동일 분석)로 포토카드 생성. 중복 URL은 기존 카드 반환, 신선도 게이트는 건너뜀, 텔레그램 알림은 안 보냄. `analyze_url()` + `extract_title()`/`extract_published()` (2026-09-02)
- [x] **F4.2-a. 그룹사 쏠림 해소** — `interleave_by_group`: 회차당 그룹사별 큐를 라운드로빈으로 뽑아 포스코퓨처엠 독점 방지. `_naver_item_relevant`: '그룹사' 키워드 결과 중 제목에 '포스코' 없는 무관 기사 제외 (2026-09-02)
- [x] **M0-4a. 언론사명 보강** — SEED_PRESS에 네이버 유입 매체 60여 개 추가. `fixpress` 명령으로 도메인 형태로 저장된 언론사명(`ajunews.com` 등)을 정식 이름으로 일괄 교체. 미등록 도메인은 TLD 제거 후 표시
- [ ] **M3-8. 신규 기사 배너** (F6.2) — "새 기사 N건" 배너. 폴링 API는 있으나 배너 UI 미구현
- [x] **M3-9. 푸터 고지** (§7-6) — 저작권 귀속 + 투자권유 아님
- [x] **M3-10. 보안 검증** (§6) — 프론트 파일에 키 없음 확인. 외부 시세 직접 호출 없음(백엔드 경유)

---

## M4. 시세 티커

**완료 기준**: 장중 60초 갱신, 조회 실패 시에도 화면 유지 (§9)

- [x] **M4-1. 대상 6종** (F9.1) — 포스코홀딩스(`005490`) · 포스코퓨처엠(`003670`) · USD · CNY · JPY(100엔) · EUR
- [x] **M4-2. 백엔드 조회 및 캐시** (F9.2) — `market_quotes` upsert. 프론트는 `/api/quotes`만 호출
- [x] **M4-3. 갱신 주기 분리** (F9.2) — 수집 루프에서 60초마다 갱신. 프론트도 60초 폴링
- [x] **M4-4. 실패 시 동작** (F9.2) — 조회 성공분만 upsert → 실패 시 마지막 값 유지. 15분 이상 낡으면 '지연' 배지. 프론트도 조회 실패 시 화면 유지
- [x] **M4-5. 표시** (F9.3) — 값 + 등락률, 상승 ▲빨강 / 하락 ▼파랑, 마지막 갱신 시각
  - [x] 환율 소스 2개 폴백 (`prices` 실패 시 `api.stock`)

---

## M5. 텔레그램 봇

**완료 기준**: 중복 발송 0건, 5분 내 도달 (§9)

- [x] **M5-1. 발송 트리거** (F7.1) — 신규 저장 + 분석 후 `queue_notification` 적재, `send_notifications`가 배출
- [x] **M5-2. 메시지 포맷** (F7.2) — 웹 카드와 동일 형식
  - [x] 중요도 이모지 🔴(80+) / 🟠(50–79)
  - [x] `[언론사, 기자]` 머리표 + 요약 + `포스코 관점:` 단락
  - [x] `parse_mode: HTML`, 제목·기자명의 `<` `>` `&` 이스케이프
  - [x] 링크 미리보기 활성
  - [x] SWOT·키워드 칩 미포함 (웹 전용)
- [x] **M5-3. 발송 정책** (F7.3)
  - [x] 묶음 발송: 대상 3건 이상이면 다이제스트 1건
  - [x] Rate limit: 큐에서 1.1초 간격 배출
  - [x] 야간 모드 23:00–07:00 — 중요도 80 이상만 즉시
  - [x] 실패 시 재시도 3회 → `status = 'failed'`
  - [x] 중요도 임계값(기본 50) 미만은 `skipped`
- [x] **M5-3a. chat_id 확정** — `chatid` 명령으로 `7575759042`(개인 DM) 확인 후 `.env` 반영. `sendtest`로 시험 발송 성공, 실제 기사 알림도 발송 확인
- [x] **M5-5. 중복 발송 방지** (§6 정합성) — `notifications` UNIQUE(article_id, channel, chat_id)
- [x] **M5-4. 텔레그램 챗봇** (F7.4 + 자연어) — `run` 모드에 getUpdates 롱폴링 스레드(`telegram_bot_loop`) 추가 (2026-09-02)
  - 명령어: `/help` `/latest` `/today` `/threshold [n]` `/filter [카테고리]` `/start`(알림 재개) `/stop`(알림 일시중지)
  - 자연어 질문 → 최근 7일 기사 40건을 컨텍스트로 LLM이 답변 (`chat_text`, JSON 강제 없음). chat별 시간당 30회 제한
  - 기사 URL 전송 → `analyze_url`로 즉시 분석해 카드 회신
  - `run_state`에 `notify_paused`·`notify_threshold`·`tg_offset` 컬럼 추가. `/threshold`·`/stop`이 런타임에 알림 동작 조정
  - 단일 채팅 유지: 브로드캐스트는 `.env`의 `TELEGRAM_CHAT_ID`로만, 명령어 응답은 보낸 사람에게

---

## M6. 운영 강화 (미착수)

- [x] **M6-1. 네이버 검색 API 소스 추가** (F1.2, §12-4) — **키 입력·검증 완료 (2026-09-02)**
  - 네이버 검색 API가 **NAVER Cloud Platform > NAVER API HUB**로 통합됨 (developers.naver.com 아님)
  - 엔드포인트 `https://naverapihub.apigw.ntruss.com/search/v1/news`, 헤더 `X-NCP-APIGW-API-KEY-ID` / `X-NCP-APIGW-API-KEY`
  - 하루 25,000회 한도 → `NAVER_INTERVAL_SEC=300` 스로틀 (29키워드 × display 30 → 하루 약 8,400회)
  - 검증: 실행당 네이버에서 ~880건 유입, 게이트 통과 후 저장·분석 확인

### M6-1 이후 반영한 파이프라인 보강 (2026-09-02)
- [x] **본문 임시 보관 테이블 `article_bodies`** (§7-3) — 분석 대기 기사의 본문을 임시 저장. 분석 완료 시 삭제, 30일 후 정리. 재분석 시 재수집 불필요
- [x] **분석 백로그 드레인** — 1회 실행 예산(`LLM_PER_RUN=6`) 안에서 밀린 기사를 처리. 백로그가 예산보다 크면 신규 수집을 예산 절반으로 조여 백로그를 해소
- [x] **G3 병렬 프리페치** — 리다이렉트 해제+HTML을 6워커로 병렬 수집. 1회 실행 145초 → 37~65초
- [x] **부트스트랩 억제 지속 + `bootstrap_at` 가드** — 억제 모드를 파이프라인 안정(신규 후보<20, 신규<10, 백로그<10)까지 유지. 추가로 `run_state.bootstrap_at`(최초 실행 시각)을 기록해 **그 이전 발행 기사는 어떤 경우에도 알림하지 않음** — 초기 수집·복구 시 과거 기사 알림 폭탄 원천 차단
- [x] **임베딩 폭주 방어** — 중복 판정 4단계 임베딩을 1회 실행당 30건으로 제한, 프리필터 문턱 0.45 → 0.62
- [x] **라이브러리 로그 잡음 억제** — readability/httpx/openai 로거를 WARNING으로
- [ ] **M6-2. 운영 대시보드** (F8) — 최근 24시간 수집 성공률, 소스별 수집량, LLM 실패, 발송 실패
- [ ] **M6-3. 경보** (F8) — 수집 10분간 0건이면 운영자 경보
- [ ] **M6-4. 관리자 화면** (F8) — 기사 숨김, 언론사 승인/차단, 키워드 추가·비활성, 수동 URL 즉시 분석
- [ ] **M6-5. `url_ledger.hit_count` 상위 항목 주기 점검** (F1.1)
- [x] **M6-6. 실행 시간 감시** (§6) — 1회 실행 20초 초과 시 경고 로그 + 다음 주기 간격 조정
- [ ] **M6-7. 가용성 측정** (§6) — `collection_logs`로 가용률 산출
- [ ] **M6-8. 발행→도달 지연 측정** (§6, §10) — `published_at` → `notifications.sent_at` P90

---

## M7. 최종 점검 및 최적화

- [ ] **M7-1. 중복 전수 점검** — 데이터 축적 후
  - [ ] 기사 중복 — 통신사 전재 15건 샘플 → 대표 1건
  - [x] 칩 중복 — 실데이터 7건 렌더링 후 0건 확인
  - [x] 발송 중복 — UNIQUE 제약 + `queue_notification` 반환값 확인
  - [x] URL 중복 — 재실행 시 기존 기사 alias 처리, HTTP 0회
  - [x] 코드 중복 — 요약 포맷 조립(`format_summary_header`)이 웹·텔레그램 공용
- [x] **M7-2. 재조회 비용 0 검증** (§6) — **통과**. 신규 0건 실행에서 외부 HTTP = 소스조회분만
- [ ] **M7-3. 성능 최적화**
  - [x] 실행당 목표 20초 → 실측 1.4~3.8초
  - [ ] 목록 조회 — 현재 `list_articles`가 최대 3000행 스캔 후 애플리케이션 필터. Supabase 전환 시 RPC로 개선 필요
  - [ ] 프론트 LCP 측정
- [x] **M7-4. 보안 최종 점검** (`env-secret-guard` §6)
  - [x] 하드코딩된 키 흔적 탐색 — 0건
  - [x] `git ls-files | grep '^\.env'` → `.env` 미추적
  - [x] `.gitignore`에 `*.db` 추가 (기사 원문 포함)
  - [x] 키 누락 시 시작 단계에서 명확한 메시지와 함께 종료
- [x] **M7-5. 저작권 준수 확인** (§7) — 본문 전문 미공개, 요약 재서술, 네이버 공식 API만, 푸터 고지
- [ ] **M7-6. §10 성공 지표 전 항목 측정** — 데이터 축적 후

---

## 남은 작업 요약

| 우선순위 | 항목 | 블로커 |
|---|---|---|
| 1 | 네이버 검색 API 키 (M6-1) | 사용자 발급 필요 (§12-4) — 현재 수집량이 얇은 근본 원인 |
| 2 | 지역 키워드 확정 (§12-3) | 사업장 소재지 확정 필요 |
| 3 | 봇 명령어 (M5-4) | 없음 — 구현 가능 |
| 4 | 신규 기사 배너 (M3-8) | 없음 — 구현 가능 |
| 5 | 운영 대시보드·관리자 화면 (M6-2~4) | 없음 |
| 6 | 배포 위치 확정 (0-6) | 사용자 결정 |
| 7 | Supabase 전환 (0-4) | 키 3개 발급 후 `DB_BACKEND=supabase` |

---

## 진행 원칙

1. 마일스톤은 순서대로. M1 검증 없이 M2로 넘어가면 LLM 비용이 샌다.
2. 각 마일스톤 완료 시 검증 3회 이상 후 보고 (CLAUDE.md 규약).
3. 오류 발생 시 원인 최소 5회 확인 후 수정, 검증 3회 이상 (CLAUDE.md 규약).
4. 새 파이썬 라이브러리는 사용 전 사용자 확인 (0-2 목록 밖).
5. API 키는 `.env`에서만. 코드·문서·커밋에 값을 쓰지 않는다.

## PRD ↔ PLAN 대응 확인표

| PRD 조항 | PLAN 항목 | 상태 |
|---|---|---|
| G1 1분 주기 탐지 | M1-1, M1-2 | ✅ |
| G2 3~5줄 요약 + 영구 저장 | M2-3, M0-1 | ✅ |
| G3 포토카드 웹 아카이브 | M3-2, M3-4 | ✅ |
| G4 텔레그램 자동 발송 | M5-1, M5-2 | ✅ |
| G5 저작권 범위 공개 | M3-9, M7-5 | ✅ |
| G6 실시간 주가·환율 | M4 전체 | ✅ |
| G7 키워드 칩·그룹사·SWOT | M2-4, M2-6, M3-4~6 | ✅ |
| F1.1 게이트 순서·전체 기간 판정 | M1-2, M1-3, M1-6, M1-10 | ✅ |
| F1.2 수집 소스 | M1-1, M6-1 | 🔶 네이버 키 대기 |
| F1.3 키워드 셋 | M0-3 | ✅ |
| F2.1 URL 정규화 2시점 | M1-4 | ✅ |
| F2.2 중복 판정 4단계 | M1-5 | ✅ |
| F2.3 언론사 식별 | M0-4 | ✅ |
| F3 분류·중요도 스코어 | M1-8 | ✅ |
| F4.1 요약 규격·표시 형식 | M2-3 | ✅ |
| F4.2 키워드·그룹사 | M2-4 | ✅ |
| F4.3 감성 태그 | M2-5 | ✅ |
| F4.4 SWOT | M2-6 | ✅ |
| F4.5 처리 방식·폴백 | M2-1, M2-8 | ✅ |
| F4.6 비용 관리 | M2-7, M2-9 | ✅ |
| F5 데이터 모델 · RLS | M0-1, M0-2 | ✅ |
| F6.1 / F6.1a 레이아웃·다중 필터 | M3-2, M3-3 | ✅ |
| F6.2 / F6.2a / F6.2b 카드·툴팁·칩 중복 | M3-4~6 | ✅ (M3-8 배너 제외) |
| F6.3 성능 | M3-1, M3-7 | 🔶 LCP 미측정 |
| F7 텔레그램 | M5 전체 | ✅ (M5-4 명령어 제외) |
| F8 운영·모니터링 | M6-2~4 | ⬜ |
| F9 시세 티커 | M4 전체 | ✅ |
| §6 재조회 비용 0 | M1-10, M7-2 | ✅ |
| §6 실행 시간 20초 | M6-6 | ✅ |
| §6 보안 | M0-5, M3-10, M7-4 | ✅ |
| §7 저작권 | M2-1, M3-9, M7-5 | ✅ |
| §8 스택·폴더 구조 | 0-1, 0-2 | ✅ |
| §12 미결정 사항 | 0-6, 0-7 | 🔶 일부 확정 |
