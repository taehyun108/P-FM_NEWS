# PC 이사 가이드 (이 문서 하나만 보고 따라하면 됩니다)

작성일: 2026-09-15

## 0. 먼저 안심하세요 — 제일 중요한 사실

**지금 쓰고 있는 뉴스 사이트(웹사이트)는 이 PC 안에서 돌아가는 게 아닙니다.**
사이트는 이미 **AWS(아마존 클라우드)** 라는 곳에 있는 서버 컴퓨터 안에서
24시간 계속 돌아가고 있습니다. 그래서:

- ✅ 이 PC를 꺼도, 다른 PC로 바꿔도 **사이트는 계속 켜져 있습니다.**
- ✅ 기사 수집, 텔레그램 알림, 주간 이메일도 **끊기지 않습니다.**
- ✅ "PC 이사"란, 사실 이 사이트를 **관리하고 코드를 고칠 때 쓰는 도구들을
  새 PC에도 똑같이 깔아주는 것**뿐입니다.

즉 이 문서의 목적은 딱 하나 — **"새 PC에서도 예전 PC처럼 관리/개발을
할 수 있게 만들기"** 입니다.

---

## 1. 이사 전에, 예전 PC에서 딱 하나만 챙기세요

예전 PC의 이 폴더 안에 있는 **`.env` 파일 하나**를 USB나 카카오톡 '나에게
보내기', 개인 클라우드(구글 드라이브 등) 중 편한 방법으로 복사해 두세요.

```
P-FM_NEWS/.env
```

> ⚠️ 이 파일 안에는 API 키(비밀번호 같은 것)가 들어 있습니다. 이 파일을
> 절대로 깃허브(GitHub)에 올리거나, 단체 카톡방 같은 곳에 공유하지 마세요.
> 나 혼자만 볼 수 있는 방법(USB, 개인 이메일 등)으로만 옮기세요.

이 파일이 필요한 이유: 예전 PC에서 이미 발급받아 넣어둔 각종 API 키
(OpenAI, Gemini, Grok, NVIDIA, 네이버, 텔레그램 등)가 이 파일 안에만
저장돼 있기 때문입니다. 이 파일만 있으면 새 PC에서 키를 하나하나 다시
발급받을 필요가 없습니다.

**또한 다음 2가지 정보도 어딘가에 적어서 같이 옮기세요** (AWS 콘솔
`console.aws.amazon.com` → IAM → 사용자 → `P-FM-NEWS-deploy` → "보안 자격
증명" 탭에서 확인 가능. 이미 발급받은 적이 있다면 그때 받은 값을 찾아서
쓰면 됩니다):

- AWS **액세스 키 ID** (Access Key ID)
- AWS **비밀 액세스 키** (Secret Access Key)

> 만약 이 두 값을 잃어버렸다면 걱정하지 마세요. AWS 콘솔에서
> "새 액세스 키 만들기"를 누르면 새로 발급받을 수 있습니다(기존 것은
> 삭제하지 않아도 됩니다).

---

## 2. 새 PC에 프로그램 3개 설치하기

아래 3가지 프로그램을 새 PC에 설치합니다. 각각 공식 홈페이지에서
"다운로드" 버튼만 누르면 되는 아주 평범한 설치 과정입니다.

| 순서 | 프로그램 | 왜 필요한가 |
|---|---|---|
| 1 | **Git** | 깃허브에 있는 코드를 내 PC로 내려받기(clone) 위해 |
| 2 | **Python** (3.11 이상) | 코드를 직접 실행/테스트해보기 위해 |
| 3 | **AWS CLI** | 클라우드 서버에 새 코드를 재배포(redeploy)하기 위해 |

설치가 끝나면 새 PC에서 명령 프롬프트(또는 PowerShell, 터미널)를 열고
아래 명령을 하나씩 쳐서 잘 설치됐는지 확인합니다. 버전 번호 같은 게
나오면 성공입니다.

```
git --version
python --version
aws --version
```

---

## 3. 코드 내려받기 (Clone)

터미널에서 코드를 받고 싶은 폴더로 이동한 뒤, 아래 명령을 실행합니다.

```bash
git clone https://github.com/taehyun108/P-FM_NEWS.git
cd P-FM_NEWS
git checkout claude/prd-skills-setup-yqehgi
```

> 마지막 줄은 지금까지 작업해온 브랜치(작업 갈래)로 이동하는 명령입니다.
> 이걸 안 하면 예전 PC에서 하던 작업 내용이 안 보일 수 있습니다.

---

## 4. 아까 챙긴 `.env` 파일 넣기

1번에서 옮겨온 `.env` 파일을 방금 내려받은 `P-FM_NEWS` 폴더 맨 위(즉
`backend`, `frontend` 폴더와 같은 위치)에 그대로 복사해서 붙여넣습니다.

- 만약 `.env` 파일을 못 챙겼거나 새로 만들어야 한다면, 같은 폴더에 있는
  `.env.example` 파일을 복사해서 이름을 `.env`로 바꾼 뒤, 안에 있는
  빈 칸들을 각 서비스 홈페이지에서 발급받은 키 값으로 채우면 됩니다.
  (단, 이 방법은 키를 전부 다시 발급받아야 해서 번거로우니 가능하면
  1번처럼 파일을 그대로 옮기는 걸 추천합니다.)

---

## 5. AWS 자격증명 등록하기 (서버 관리용 — 꼭 필요할 때만)

**"사이트 코드를 고쳐서 실제 서버에 반영하고 싶을 때"만** 필요한
단계입니다. 단순히 코드만 보거나 로컬에서 테스트만 할 거라면 이 단계는
건너뛰어도 됩니다.

터미널에서 아래 명령을 치면, 순서대로 몇 가지를 물어봅니다.

```bash
aws configure
```

물어보는 항목과 넣을 값:

1. `AWS Access Key ID` → 1번에서 챙긴 액세스 키 ID
2. `AWS Secret Access Key` → 1번에서 챙긴 비밀 액세스 키
3. `Default region name` → `ap-northeast-2` 입력 (서울 리전)
4. `Default output format` → 그냥 Enter (비워둬도 됨)

> ⚠️ 새로운 AWS 계정이나 새로운 IAM 사용자를 만들 필요가 **전혀
> 없습니다.** 예전 PC에서 쓰던 `P-FM-NEWS-deploy`라는 계정을 그대로
> 재사용하는 것입니다. 이 계정에는 이번 프로젝트에 필요한 권한만 딱
> 맞게 이미 설정이 다 되어 있기 때문에, 새로 만들면 오히려 권한 설정을
> 처음부터 다시 해야 해서 훨씬 번거롭습니다.

---

## 6. 새 PC에서 서버 재배포해보기 (테스트)

코드를 수정한 뒤 실제 운영 서버에 반영하고 싶을 때는, 아래 한 줄이면
끝입니다.

```bash
bash deploy/redeploy.sh
```

이 스크립트가 자동으로 해주는 일:

1. 새 코드로 이미지를 클라우드(AWS CodeBuild)에서 빌드
2. 완성된 이미지를 저장소(ECR)에 올리기
3. 실제 서버(EC2)에서 그 이미지를 받아 재시작
4. 설정값(API 키 등)도 AWS에 저장된 최신 것으로 다시 적용

**로컬 PC에 Docker를 설치할 필요가 전혀 없습니다** — 빌드는 전부 AWS
클라우드 안에서 일어납니다. 이 스크립트가 오류 없이 끝까지 실행되면
새 PC에서도 예전 PC와 똑같이 서버를 관리할 수 있다는 뜻입니다.

---

## 7. 로컬에서 코드 테스트해보기 (개발용, 선택사항)

코드를 고치기 전에 내 PC에서 먼저 돌려보고 싶다면:

```bash
cd P-FM_NEWS
python backend/main.py selftest
```

맨 마지막 줄에 `전체 통과.`가 뜨면 정상입니다.

실제로 화면을 띄워서 보고 싶다면:

```bash
python backend/main.py serve
```

그 다음 웹 브라우저에서 `http://127.0.0.1:8000` 으로 들어가면 됩니다.

---

## 8. 정리 — 새 PC 준비 체크리스트

- [ ] 예전 PC의 `.env` 파일을 안전하게 옮겨왔다
- [ ] AWS 액세스 키 ID / 비밀 액세스 키를 확보했다
- [ ] Git, Python, AWS CLI 설치를 마쳤다
- [ ] `git clone` 으로 코드를 받고, `claude/prd-skills-setup-yqehgi`
      브랜치로 이동했다
- [ ] `.env` 파일을 프로젝트 폴더에 넣었다
- [ ] `aws configure` 로 자격증명을 등록했다
- [ ] `bash deploy/redeploy.sh` 가 오류 없이 끝까지 실행됐다

여기까지 다 됐다면, 새 PC에서도 예전 PC와 완전히 똑같이 이 프로젝트를
관리하고 개발할 수 있습니다. 실제 운영 중인 사이트는 이 과정 내내
단 1초도 멈추지 않습니다 — 전부 AWS 안에서 계속 돌아가고 있었기
때문입니다.
